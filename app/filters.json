{
    "drop": [
        {
            "field": "limit",
            "default": 100
        }
    ],
    "keep only fields": [
        {
            "field": "field",
            "default": "type"
        },
        {
            "field": "value",
            "default": "Park"
        }
    ],
    "drop fields": [
        {
            "field": "field",
            "default": "type"
        },
        {
            "field": "value",
            "default": "Park"
        }
    ]
}